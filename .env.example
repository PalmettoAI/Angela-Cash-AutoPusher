# Postgres connection (local docker-compose by default)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/autopusher

# Pino log level (debug, info, warn, error)
LOG_LEVEL=info

# Node environment
NODE_ENV=development
